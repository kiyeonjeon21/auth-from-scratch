# 00. 첫 로그인 트레이스

**이 챕터에서만 라이브러리를 쓴다.**

목적은 구현이 아니라 관찰이다.
`coreos/go-oidc` + `golang.org/x/oauth2`로 로그인 하나를 끝까지 성공시키고, 그 과정의 모든 HTTP 왕복을 주석이 달린 `trace.md`로 떨군다.
01 이후 챕터는 여기서 나온 질문을 하나씩 갚아나가는 작업이다.

---

## 그림부터

코드를 돌리기 전에 이 세 장을 순서대로 본다.
채널 분리는 본질적으로 공간적인 개념이라 글로 읽는 것보다 그림이 훨씬 빠르다.

**[1. 가장 쉬운 그림 - 로그인이란](https://app.excalidraw.com/s/AU3bkHPBsIE/4o7ZsmOJtq2)**
프로토콜 용어가 하나도 없다. `가게` / `너` / `확인소` 셋만 나온다.
가게가 비밀번호를 한 번도 못 본다는 것, 너를 지나간 건 교환권뿐이라는 것.
이 두 가지만 가져가면 된다.

**[2. 로그인 한 번에 무슨 일이 일어나나](https://app.excalidraw.com/s/AU3bkHPBsIE/VeJ6rXc0py)**
1번과 **레이아웃이 완전히 같다.** 상자 글씨만 진짜 이름으로 바뀐다.
레인이 `앱 | 브라우저 | Keycloak` 순서고 브라우저가 가운데다.
프론트채널은 가운데를 거쳐 두 번 꺾여 가고, 백채널은 가운데를 건너뛴다.
백채널 구간엔 브라우저 상자가 하나도 없다. 그게 핵심이다.

**[3. PKCE - 가루와 원본](https://app.excalidraw.com/s/AU3bkHPBsIE/74Xaul0gvdJ)**
빨간 막대 화살표 하나만 보면 된다.
가루에서 원래 단어를 되돌릴 수 없다는 것, 그게 PKCE가 동작하는 이유 전부다.

전체 목록은 `notes/diagrams.md`.

---

## 실행

```bash
make kc-up      # 로컬 IdP 기동 (demo realm 자동 임포트)
make run-00     # 앱 기동
```

브라우저에서 http://localhost:5556 을 연다.
`alice` / `alice` 로 로그인하면 `00-first-login-trace/trace.md` 가 생성된다.

브라우저 개발자도구 네트워크 탭에서 **Preserve log**를 켜두고 로그인하면, 앱이 못 보는 브라우저-IdP 구간까지 같이 볼 수 있다.

---

## 무엇부터 보나

`trace.md` 맨 위의 **"먼저 볼 것"** 부터 본다.
왕복 572줄을 처음부터 읽으면 무엇이 중요한지 알 수 없다.

거기 있는 것은 미리 써둔 문장이 아니라 **이번 실행의 실제 데이터에서 계산된 것**이다.
`aud`가 실제로 없을 때만 "없다"고 하고, `auth_time`이 실제로 벌어졌을 때만 재인증 생략을 지적한다.
조건이 안 맞으면 그 항목은 아예 나오지 않는다.

로그인 직후 브라우저 화면에도 같은 내용이 뜬다.

---

## 무엇이 기록되나

`trace.md`는 왕복을 두 채널로 나눠서 보여준다.
이 구분이 OAuth 구조에서 제일 중요한 사실이다.

| 채널 | 어디를 지나나 | 누가 볼 수 있나 |
|---|---|---|
| front | 브라우저를 경유하는 리다이렉트와 쿼리스트링 | 사용자, 확장 프로그램, Referer, 브라우저 히스토리 |
| back | 서버 대 서버 직접 호출 | 두 서버만 |

시크릿이 나와도 되는 자리는 back 하나뿐이다.
`trace.md`를 보면서 "이 값이 왜 이쪽 채널에 있나"를 계속 물어야 한다.

파라미터마다 "왜 있나" 열이 붙어 있고, 용어집에 없는 값은 `**TODO**`로 표시된다.
**그 TODO가 이 챕터의 실제 과제다.**

---

## 체크리스트

- [ ] `make kc-up`으로 IdP 기동, `make discovery`로 디스커버리 문서 확인
- [ ] 로그인 성공시키고 `trace.md` 생성
- [ ] `trace.md`의 요약 표를 보고 왕복 순서를 말로 설명해보기
- [ ] `**TODO**` 항목을 전부 찾아서 답 찾기
- [ ] 브라우저 개발자도구로 브라우저-IdP 구간까지 확인
- [ ] `ANSWERS.md` 채우기

---

## 답할 수 있어야 하는 질문

- 로그인 한 번에 프론트채널 몇 번, 백채널 몇 번이 오가나?
- `code`는 왜 프론트채널로 와도 되고, `client_secret`은 왜 안 되나?
- 라이브러리가 대신 해준 것과, 여전히 내가 직접 해야 하는 것은 각각 무엇인가?
- ID 토큰에는 `aud`가 있는데 액세스 토큰에는 없다. 리소스 서버는 그럼 무엇을 보고 자기에게 온 토큰이라 판단하나?
- `azp`는 `aud`와 무엇이 다른가? `azp`만 있으면 충분한가?
- `code_challenge`는 프론트채널로 나가고 `code_verifier`는 백채널로 나간다. 이 비대칭이 무엇을 사주나?
- 디스커버리 문서 한 번으로 대체된 하드코딩은 몇 개인가? 그 대신 무엇을 신뢰하게 되었나?

---

## 함정

**`redirect_uri` 불일치**
IdP에 등록된 값과 완전히 같아야 한다.
포트를 바꿨으면 `docker/keycloak/realm-demo.json`도 같이 고치고 `make kc-reset`.

**포트 5556**
3000은 다른 개발 서버와 자주 충돌한다.
충돌하면 `redirect_uri` 불일치로 나타나서 원인을 찾기 어렵다.

**`trace.md`는 커밋하지 않는다**
로컬 IdP가 발급한 진짜 토큰이 들어있다.
`.gitignore`에 이미 들어가 있다.
