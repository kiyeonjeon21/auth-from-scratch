# 00. 답변

스펙 문장을 옮겨 적지 않는다.
자기 말로 쓰고, 못 쓰겠으면 아직 모르는 것이다.
`trace.md`의 몇 번 왕복을 근거로 하는지 같이 적는다.

---

### 로그인 한 번에 프론트채널 몇 번, 백채널 몇 번이 오가나?

> (미작성)

### `code`는 왜 프론트채널로 와도 되고, `client_secret`은 왜 안 되나?

> (미작성)

### 라이브러리가 대신 해준 것과, 여전히 내가 직접 해야 하는 것은?

> (미작성)

### ID 토큰에는 `aud`가 있는데 액세스 토큰에는 없다. 리소스 서버는 무엇을 보고 자기 것이라 판단하나?

> (미작성)

### `azp`는 `aud`와 무엇이 다른가? `azp`만 있으면 충분한가?

> (미작성)

### `code_challenge`는 프론트, `code_verifier`는 백. 이 비대칭이 무엇을 사주나?

> (미작성)

### 디스커버리 문서 한 번으로 대체된 하드코딩은 몇 개인가? 대신 무엇을 신뢰하게 되었나?

> (미작성)

---

## trace.md에서 찾은 TODO

용어집에 없어서 `**TODO**`로 뜬 값들.
찾은 답을 여기 적고, 재사용할 만하면 `internal/wiretrace/glossary.go`에 넣는다.

| 값 | 어디서 나왔나 | 무엇인가 |
|---|---|---|
| | | |
