# 방식 비교표

이 저장소의 결과물이다.

챕터 하나를 끝낼 때마다 여기 한 줄이 채워진다.
개별 방식은 다른 데서도 배울 수 있지만, **이 표는 직접 만들어봐야 생긴다.**

빈 칸을 추측으로 채우지 않는다. 구현해서 확인한 것만 적는다.

---

## 축

| 열 | 묻는 것 |
|---|---|
| 상태 위치 | 로그인했다는 사실을 누가 기억하나 |
| 증명 대상 | 아는 것(knowledge) / 가진 것(possession) / 그 사람인 것(inherence) |
| 훔치면 끝? | 값만 복사하면 남이 쓸 수 있나 (bearer 여부) |
| 취소 | 발급한 걸 즉시 무효화할 수 있나 |
| 신뢰의 뿌리 | 결국 무엇을 믿기로 하는가 |
| 피싱 저항 | 가짜 사이트에 속아도 안전한가 |

---

## 표

| 방식 | 상태 위치 | 증명 대상 | 훔치면 끝? | 취소 | 신뢰의 뿌리 | 피싱 저항 | 챕터 |
|---|---|---|---|---|---|---|---|
| 세션 + 쿠키 | | | | | | | A1 |
| JWT bearer | 없음 (자기 완결) | 아는 것 | 예 | 어려움 | 발급자 서명키 | 없음 | A2 → lab |
| OIDC 로그인 | IdP + 클라이언트 | 아는 것 | 예 | 부분적 | IdP 서명키 (JWKS) | 없음 | D1 (00·02) |
| 로그아웃 후 상태 | | | | | | | A3 |
| TOTP / 매직링크 | | | | | | | B2 |
| Passkey (WebAuthn) | | | | | | | B3 |
| HMAC 요청 서명 | | | | | | | C2 |
| DPoP | | | | | | | C3 |
| SAML | | | | | | | D2 |

JWT bearer와 OIDC 두 줄은 00·02와 `agent-identity-lab`에서 확인한 것이다.
나머지는 아직 비어 있다.

---

## 표를 채우며 답할 질문

한 줄씩 채울 때마다 이것부터 답한다. 답이 안 나오면 그 방식을 아직 이해 못 한 것이다.

- **이 방식은 앞의 무엇이 부족해서 나왔나?**
  인증의 역사는 "이게 깨져서 저게 나왔다"의 연속이다. 앞 줄과의 차이가 그 방식의 존재 이유다.
- **무엇을 포기했나?**
  공짜는 없다. 상태를 버리면 취소를 잃고, 취소를 얻으면 확장성을 잃는다.
- **이 줄에서 훔칠 수 있는 값은 무엇이고, 훔치면 무엇을 할 수 있나?**

---

## 지금까지 확인한 것

**bearer 토큰의 대가**
JWT와 OIDC 액세스 토큰은 둘 다 bearer다. 가진 사람이 곧 주인이라 훔치면 그대로 쓸 수 있고,
자기 완결적이라 만료 전에 죽이기가 어렵다. 수명을 짧게 잡는 것 말고는 방어가 없다.
C3(DPoP)이 이 전제를 깨는 방향이다.

**`aud` 없는 액세스 토큰**
Keycloak 기본 설정은 액세스 토큰에 `aud`를 넣지 않는다 (00 발견 2번).
리소스 서버가 토큰만 보고는 자기 것인지 판단할 수 없다는 뜻이다.

**서명을 안 보면 나머지 검증이 전부 무의미하다**
02는 `iss`/`aud`/`exp`/`nonce`를 다 검증하지만 서명은 03으로 미뤘다.
그 상태로는 공격자가 지어낸 토큰도 전부 통과한다. 검증 순서에는 위계가 있다.

**라이브러리가 해주던 일은 대부분 왕복이 아니라 계산이었다**
00(라이브러리)과 02(손으로)의 트레이스를 비교하니 네트워크 차이가 딱 하나였다.

```
백채널 호출:  00 = 3번,  02 = 2번
없어진 것:    JWKS 가져오기
```

`state`/`nonce` 생성, PKCE 해시, 폼 조립, 클레임 대조는 전부 네트워크에 안 남는다.
그래서 트레이스만 보면 두 챕터가 거의 같아 보이는데 코드 양은 전혀 다르다.
**와이어 캡처로 볼 수 있는 것과 볼 수 없는 것의 경계**를 보여주는 사례다.
앞으로 다른 방식을 비교할 때도 트레이스가 같다고 같은 게 아니라는 걸 기억할 것.
