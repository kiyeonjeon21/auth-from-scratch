# 그림

Excalidraw 워크스페이스 `kiyeon lab` 의 **`auth-from-scratch`** 컬렉션.

챕터를 진행하면서 하나씩 추가한다.
글로 설명하기 어려운 것, 특히 **공간적인 개념**(무엇이 어디를 지나가나, 무엇이 어디에 없나)만 그린다.
글로 충분한 것은 그리지 않는다.

---

## 지금 있는 것

| 챕터 | 제목 | 무엇을 보여주나 |
|---|---|---|
| 00 | [로그인 한 번에 무슨 일이 일어나나](https://app.excalidraw.com/s/AU3bkHPBsIE/VeJ6rXc0py) | 프론트채널과 백채널의 구조적 차이 |
| 00 | [PKCE - 가루와 원본](https://app.excalidraw.com/s/AU3bkHPBsIE/74Xaul0gvdJ) | 해시는 공개 경로로, 원본은 비공개 경로로 |

### 로그인 한 번에 무슨 일이 일어나나

레인이 `앱 | 브라우저 | Keycloak` 순서고 **브라우저가 가운데**다.
이 배치 때문에 채널 차이가 그림 구조로 드러난다.

- 프론트채널은 가운데 레인을 **거쳐** 두 번 꺾여 간다. 거치는 자리마다 "주소창에 그대로 보인다" 상자가 있다
- 백채널은 가운데를 **건너뛰고** 한 번에 간다. 그 구간엔 브라우저 상자가 하나도 없다

`client_secret` 과 진짜 단어가 8번에만 등장한다는 게 결론이다.

### PKCE - 가루와 원본

빨간 막대 화살표(`가루 ─┤ 진짜 단어`) 하나가 이 그림의 전부다.
되돌릴 수 없다는 사실이 PKCE가 동작하는 이유다.

---

## 앞으로 그릴 것

해당 챕터를 할 때 추가한다. 미리 빈 씬을 만들지 않는다.

| 챕터 | 그릴 것 | 이미 있는 재료 |
|---|---|---|
| 02 | `state` / `nonce` / PKCE 가 각각 무엇을 막나 | 발견 4번, 라이브러리 검증의 경계선 |
| 03 | 검증 순서, `kid` 로 키 고르기, `aud` 없는 토큰 | 발견 2번, 도서관/수영장 출입증 |
| 04 | 토큰을 어디에 두나, BFF | |
| 05 | 세션이 세 곳에 흩어져 있다, front/back-channel logout | 발견 3번, 손등 도장 |
| 06 | 위임 체인, `act` 중첩 | |
| 08 | DPoP 키 바인딩 | |

작업 절차는 `.claude/skills/auth-diagrams/` 에 있다.
